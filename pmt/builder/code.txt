Write the following script to builder.py:

1. If no command line arguments are provided, ask the user "What would you like to build? ", else concatenate the arguments
2. Call `handle` (import from src.request) with the user input as argument

Make sure to import the right packages
